# レシピ DAG ソルバー 仕様

目標アイテムから逆算して、必要な素材と中間生成物の有向非巡回グラフを構築し、各段階の必要量を計算する SPA。
アトリエシリーズと Factorio の両方を、共通のデータスキーマと共通のソルバーで扱う。

## 1. スコープ

### 対象

- レシピ定義（JSON）を読み込み、目標アイテムから逆方向に依存グラフを構築する
- 各中間生成物の必要量を計算する（離散モードとレートモードの二種）
- グラフを可視化し、ノードを選択して詳細を見られるようにする
- ユーザーが独自のレシピ JSON を貼り付け、またはアップロードできる
- Mermaid、DOT、CSV 形式でエクスポートできる

### 非対象（初版）

- サーバサイド処理。静的ホスティングのみで完結させる
- ゲームからのレシピ自動取得。データは同梱 JSON かユーザー投入とする
- アトリエの品質、特性の伝播計算
- 生産ラインのレイアウト最適化

## 2. 配信形態の制約

GitHub Pages の `https://<user>.github.io/<repo>/` 配下に配置する。

- Vite の `base` を `'/<repo>/'` に設定する。
- 静的アセットの参照は必ず `import.meta.env.BASE_URL` を経由する。絶対パス `/data/...` で書くと本番でのみ 404 になる。
- ルーティングはハッシュルータを使う。GitHub Pages はパスを直接叩かれると 404 を返すため、`404.html` による回避策よりハッシュのほうが確実である。
- デプロイは GitHub Actions（`actions/deploy-pages`）で `main` への push 時に自動実行する。
- 同梱データセットは `src/data/` に置き、動的 `import()` で遅延ロードする。Factorio の全レシピは数百 KB を超えうるため、初期バンドルに含めない。

## 3. データモデル

### 3.1 数値表現

**すべての数量を有理数で保持する。** 浮動小数点数は表示の直前でのみ使う。

根拠は、入力データが構造的に有理数だからである。
クラフト時間（0.5、3.2）、入出力個数（整数）、機械速度（0.5、0.75、1.25、2.5）、モジュール効果（0.04 や 0.1 刻み）のいずれも有限小数か整数である。
浮動小数点で扱うと 1/3 個/秒のようなレートが頻出する場面で誤差が乗り、機械台数の切り上げが境界でぶれる。

```ts
class Rational {
  readonly n: bigint;  // 符号は n が持つ
  readonly d: bigint;  // 常に正、gcd(n, d) = 1

  static of(n: bigint | number, d?: bigint | number): Rational;
  static fromDecimal(s: string): Rational;   // "3.2" → 16/5
  add(o: Rational): Rational;
  sub(o: Rational): Rational;
  mul(o: Rational): Rational;
  div(o: Rational): Rational;
  cmp(o: Rational): -1 | 0 | 1;
  isZero(): boolean;
  ceilToBigInt(): bigint;
  toNumber(): number;    // 表示の最終段でのみ呼ぶ
  toString(): string;    // "16/5"
}
```

実装上の要件を二つ挙げる。

演算のたびに gcd で約分する。怠るとビット長が単調に増加する。

JSON パース時に `3.2` を double 経由で読むと構築前に誤差が乗る。
スキーマ側で数値を文字列として受け取るか、`fromDecimal` で 10 進文字列から直接構築する。

### 3.2 スキーマ

```jsonc
{
  "schemaVersion": 1,
  "profile": {
    "id": "factorio-2.0",
    "name": "Factorio 2.0",
    "quantityMode": "rate",          // "discrete" | "rate"
    "allowCategoryInputs": false,
    "displayMode": "reducedPair"     // rate のときのみ有効
  },
  "items": [
    { "id": "iron-plate", "name": "鉄板", "categories": [] }
  ],
  "recipes": [
    {
      "id": "iron-gear-wheel",
      "name": "鉄の歯車",
      "outputs": [{ "item": "iron-gear-wheel", "qty": "1" }],
      "inputs":  [{ "kind": "item", "item": "iron-plate", "qty": "2" }],
      "time": "0.5",
      "machineCategory": "assembling",
      "allowProductivity": true
    }
  ],
  "machines": [
    { "id": "assembling-machine-3", "name": "組立機3", "category": "assembling", "baseSpeed": "1.25" }
  ],
  "rawItems": ["iron-ore", "coal", "water"]
}
```

数量フィールドはすべて文字列とし、`Rational.fromDecimal` で読む。

**副産物**：`outputs` は常に配列である。単一出力のレシピも要素 1 の配列で書く。

**カテゴリ材料**：アトリエの「（水）カテゴリのもの」を表現するため、`inputs` の要素は判別共用体とする。

```ts
type Ingredient =
  | { kind: 'item';     item: ItemId;     qty: Rational }
  | { kind: 'category'; category: CatId;  qty: Rational };
```

`allowCategoryInputs: false` のプロファイルで `kind: 'category'` が現れたら検証エラーとする。

### 3.3 GameProfile による差分の吸収

両ゲームの本質的な差分は三点しかない。

| | アトリエ | Factorio |
|---|---|---|
| 数量 | 離散（切り上げ、余り在庫） | レート（毎秒、実数値） |
| 材料指定 | カテゴリ指定あり | アイテム直指定のみ |
| 副産物と循環 | ほぼなし | あり |

`GameProfile` はこの差分をフラグとして持つだけで、ソルバーの実装は共通のコードパスを通る。

## 4. 選択の保留（ResolutionChoice）

グラフ展開時に、下流を確定できない箇所が二種類ある。

- 同じアイテムを作るレシピが複数存在する（Factorio の石油処理、炉種別）
- カテゴリ材料が具体アイテムに解決されていない（アトリエ）

この二つは「選択が保留されているノード」という同一の構造として扱う。

```ts
type ResolutionKey =
  | { kind: 'recipe';   item: ItemId }      // どのレシピで作るか
  | { kind: 'category'; category: CatId; atRecipe: RecipeId };

type ResolutionChoice = Map<string, string>;  // key の文字列化 → 選ばれた ID
```

未解決のノードはグラフ上に「未解決」として描画し、ユーザーが選ぶと下流が生える。
`ResolutionChoice` はグローバルに一意とする（同じアイテムを場所によって別レシピで作ることは許さない）。
これによりノードの同一性を `recipe.id` で保てる。

## 5. 機械構成とモジュール

モジュールやビーコンの効果を、派生レシピをデータに列挙する形では持たない。
レシピ数が構成数との積で膨らみ、そのすべてが「どのレシピを使うか」の選択肢に流れ込むためである。

代わりに構成を独立したデータとして持ち、レシピとの組から実効レシピを導出する。

```ts
type MachineSetup = {
  id: string;                     // "am3-prod3-8beacon"
  name: string;
  baseMachine: MachineId;
  speedMultiplier: Rational;      // 機械速度 × (1 + 速度ボーナス)
  productivityBonus: Rational;    // 0, 1/10, ...
  qualityChance?: Rational;       // 初版では未使用
};

function derive(recipe: Recipe, setup: MachineSetup): EffectiveRecipe;
```

### 5.1 速度と生産力を分離する

この二つは計算に効く場所が違う。

**速度ボーナスは物量比に影響しない。** 毎秒の産出数は変わるが、鉄板 2 枚 → 歯車 1 個という比は不変である。
したがってソルバーには渡さず、必要レートから機械台数を求める最終段でのみ使う。

**生産力ボーナスは物量比を変える。** 入力あたりの産出が増え、上流の必要量が減る。
これはソルバーの内部で扱う。

この分離により、速度構成を変更しても DAG 上の数量が再計算されないことが構造的に保証される。

### 5.2 生産力の適用可否

Factorio では生産力モジュールを挿せるレシピが限定されている。
`recipe.allowProductivity` が `false` なら `derive` は生産力を無視する。
これを怠ると中間素材以外の産出も増えてしまう。

生産力の上限値はデータセット側の定数として持ち、`derive` 内でクランプする。
バージョンによって値が変わるため、コードにハードコードしない。

### 5.3 構成の割り当て

三層のフォールバックで解決する。

1. レシピ個別の指定
2. `machineCategory` ごとの既定
3. データセット全体の既定

### 5.4 ビーコンの逓減

Factorio 2.0 のビーコン効果は複数設置時に逓減し、その式に平方根が含まれる可能性がある。
含まれる場合、結果は無理数となり有理数で閉じない。

対処は、ビーコン構成から速度ボーナスを求める計算だけを浮動小数点で行い、ゲーム内の丸め精度に合わせて丸めてから `Rational` に変換する。
速度ボーナスは物量比に影響しないため、この丸めが上流の必要量を汚染することはない。
生産力ボーナスはビーコンから来ないので影響を受けない。

正確な逓減式は実装時に一次情報で確認すること。この仕様書は式を確定していない。

## 6. ソルバー

### 6.1 グラフ展開

目標アイテムから逆方向に深さ優先探索する。
ノードはアイテム、辺は「そのレシピで消費される」関係とする。

終端条件は、レシピを持たないアイテム、または `rawItems` に含まれるアイテムである。

同じアイテムが複数経路に現れるためノードは ID で一意化する。
木ではなく DAG として構築する。歯車が複数箇所で使われる構造を潰さない。

### 6.2 循環の扱い

Factorio には循環するレシピ群が存在する（石油の分解ループ、石炭液化、コバレックス濃縮、リサイクラーのループ）。
`visited` 集合で無限展開を防ぎ、循環を構成する辺は別種としてマークする。

### 6.3 数量計算

**強連結成分に分解し、縮約グラフをトポロジカルソートする。**
循環していない部分は上流から順に有理数の乗除算を積むだけで済み、連立方程式を解く必要があるのは強連結成分の内部だけである。

Factorio の循環はいずれも 2 個から 5 個程度のレシピからなる局所的なものであり、数百ノードのグラフでも実際に消去法を回すのは小さな行列が数個だけになる。分母の桁数が問題になる規模には届かない。

**離散モード**（アトリエ）：`ceil(必要量 / レシピ産出量)` で実行回数を求め、端数の余りを在庫として次の消費に回す。

**レートモード**（Factorio）：レシピを列、アイテムを行とする生産行列 `A` を組み、目標ベクトル `b` に対し `A x = b` を解く。`x` は各レシピの毎秒実行回数である。副産物はこの形で自然に扱える。

強連結成分内の求解は素朴な有理数ガウス消去法で開始してよい。
中間項の桁数が問題になった場合は、Bareiss の分数フリー消去に差し替える。

### 6.4 機械台数

```
必要台数 = ceil( 必要レート ÷ (実効産出量 × 機械速度 ÷ 実効クラフト時間) )
```

有理数で計算するため、稼働率（「12 台のうち 3/7 台分が余剰」）も正確に出せる。
浮動小数点では境界でぶれる箇所が、有理数なら断定できる。

## 7. レートの表示

レートが既約分数 `p/q` 個/秒のとき、そのまま「q 秒で p 個」と読める。

ただし分母が大きいと人間にとって情報量がない。三つのモードを用意する。

**reducedPair**：分母が閾値（既定 60）以下なら「q 秒で p 個」、超えたら小数表示に切り替える。正確な分数は常にツールチップで見せる。

**perWindow**：グラフ全体のレートの分母の最小公倍数を取り、単一の時間窓に揃えて全ノードを表示する。すべての数値が整数になり、生産ライン全体を一枚の表として眺められる。最小公倍数が非現実的に大きい場合は次のモードに落とす。

**perSecond**：1 秒、1 分、1 時間からユーザーが選んだ窓での値を出す。端数は分数表示か小数表示とする。

## 8. ユーザー投入 JSON

### 8.1 検証

投入 JSON は壊れているものとして扱う。Zod でスキーマ検証し、エラーは可能な限り位置情報つきで返す。

検証項目を、致命的エラーと警告に分けて扱う。

**致命的**：スキーマ違反、存在しないアイテム ID の参照、`outputs` が空のレシピ、プロファイルと矛盾する `kind: 'category'`

**警告**：どのレシピからも作られず `rawItems` にも含まれないアイテム、循環の検出、どこからも参照されないレシピ

致命的エラーがなければ、警告が残っていてもグラフを描画する。部分的にでも結果を出すほうが使い勝手がよい。

### 8.2 永続化と共有

投入データは localStorage に保存する。

共有 URL については制約がある。
同梱データセットなら `#/?ds=factorio-2.0&target=...&choices=...` で完結するが、ユーザー投入データは URL に載せると数百 KB になり現実的でない。

初版では、**同梱データセット使用時のみ完全な共有リンクを発行する**。
ユーザー投入時は共有リンクを無効化し、その旨を UI で示す。

将来的な拡張として、データセットの内容ハッシュと目標設定だけを URL に入れ、開いた側が同じデータを持たない場合に読み込みを促す方式が考えられる。初版では実装しない。

## 9. 可視化

`@dagrejs/dagre` でレイアウト座標を計算し、描画は React の SVG で自前に行う。

Cytoscape.js を採用しない理由は、必要な機能がレイヤー配置、パンとズーム、ノード選択に限られ、API 表面積の大部分を使わないためである。
dagre は座標計算のみを行う軽量なライブラリで、返された座標を SVG に流し込むだけで済む。
ノードにレシピ名、必要数、機械種別を自由に描き込め、ハイライトやアイコン差し込みの実装も素直になる。

Mermaid はテキストを組み立てて渡す構造上、ノードごとの動的な情報表示や選択状態の反映が苦しい。エクスポート機能としてのみ残す。

パンとズームは CSS transform で実装する。

Factorio の全レシピを展開すると 300 から 500 ノード程度になるが、SVG でこの規模は問題ない。
1000 ノードを超える要求が出た場合に Canvas への切り替えを検討する。

### 表示要素

- ノード：アイテム名、必要量（プロファイルに応じた表示形式）、生成レシピ名、機械種別と必要台数
- 辺：消費量のラベル
- 未解決ノード：選択待ちであることを明示し、クリックで候補を提示する
- 循環辺：通常辺と区別して描画する

## 10. ディレクトリ構成

```
src/
  core/                  # DOM 非依存。Vitest でテストする
    rational.ts
    schema.ts            # Zod による検証
    index.ts             # 逆引きインデックス、循環検出
    expand.ts            # 逆方向 DFS による DAG 構築
    derive.ts            # レシピ × MachineSetup → 実効レシピ
    scc.ts               # 強連結成分分解
    solve/
      discrete.ts
      rate.ts
    display.ts           # レート表示形式の変換
    export.ts            # Mermaid / DOT / CSV
  ui/
    components/
    hooks/
    layout/              # dagre 呼び出しと SVG 描画
  data/
    factorio-2.0.json
    atelier-ryza.json
  main.tsx
```

`src/core/` は UI から完全に独立させ、先に実装とテストを固める。

## 11. 技術スタック

- Vite + React + TypeScript（strict）
- 状態管理：Zustand または React Context（規模次第）
- 検証：Zod
- レイアウト：`@dagrejs/dagre`
- テスト：Vitest
- 整形：Biome または ESLint + Prettier

## 12. 実装順序

1. `Rational` と、その単体テスト
2. `schema.ts` と検証テスト
3. `expand.ts`（循環なし、レシピ一意）＋ `solve/discrete.ts`
4. Mermaid エクスポートで結果を目視確認できる状態にする
5. `scc.ts` と `solve/rate.ts`
6. `derive.ts`（機械構成とモジュール）
7. dagre + SVG による描画
8. `ResolutionChoice` の UI
9. JSON 投入、localStorage、共有 URL
10. GitHub Actions によるデプロイ

4 の段階で、UI なしでもソルバーの正しさを確認できるようにしておく。
