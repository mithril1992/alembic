# CLAUDE.md

このリポジトリで作業するときの規約。仕様は `SPEC.md` を参照する。

## プロジェクト概要

目標アイテムから必要素材と中間生成物の DAG を構築し、必要量を計算する SPA。
アトリエシリーズと Factorio を共通のスキーマとソルバーで扱う。
GitHub Pages でホストする静的サイトであり、バックエンドは存在しない。

## 絶対に守ること

### 数値は Rational で扱う

`number` で数量を計算してはいけない。
レシピの数量、クラフト時間、モジュール効果、計算途中のすべてのレートを `Rational`（BigInt ベース）で保持する。

`toNumber()` を呼んでよいのは、UI に表示する直前と、dagre にレイアウト座標を渡すときだけである。
`core/` の内部で `toNumber()` が現れたら設計を疑うこと。

JSON の数量フィールドは文字列である。`Rational.fromDecimal` で読む。
`parseFloat` を経由させない。誤差が構築前に乗る。

### base path

静的アセットの参照は `import.meta.env.BASE_URL` を経由する。
絶対パス（`/data/...`）で書くと開発環境では動き、GitHub Pages でのみ 404 になる。

ルーティングはハッシュルータを使う。BrowserRouter に変更しない。

### core は DOM に依存しない

`src/core/` から React、`window`、`document`、`localStorage` を参照しない。
このディレクトリは Node 上で単体テストできる状態を保つ。

### 速度ボーナスをソルバーに入れない

速度ボーナスは物量比に影響しない。必要量の計算には渡さず、機械台数を求める最終段でのみ使う。
生産力ボーナスは物量比を変えるのでソルバー内で扱う。

この分離を崩すと、速度構成を変えただけで上流の必要量が変わるという誤った挙動になる。

## 実装上の注意

### 有理数の約分

四則演算のたびに gcd で約分する。怠るとビット長が単調増加する。
`Rational` のコンストラクタは private にし、正規化を必ず通す。

### レシピの多重性

1 アイテムに対して生成レシピは複数ありうる。`Map<ItemId, Recipe[]>` で持つ。
`Map<ItemId, Recipe>` にしてはいけない。Factorio の石油処理や炉種別で破綻する。

### 循環

Factorio のレシピグラフは循環を含む。トポロジカルソートが常に成功する前提を置かない。
強連結成分に分解し、成分内部だけを連立方程式で解く。

### outputs は常に配列

単一出力のレシピも要素 1 の配列で書く。副産物と品質モジュールがこの形を要求する。

### 生産力の適用可否

`recipe.allowProductivity` が false のレシピに生産力を適用しない。
上限値はデータセット側の定数から読む。コードにハードコードしない。

### 事実の確認

Factorio の数値（ビーコンの逓減式、生産力の上限、各機械のベース速度、レシピの詳細）を記憶から書かない。
不確かな箇所はコード中に `// TODO: 要検証` を残し、データセット側の定数として外に出す。
仕様書に書いていない数値を推測で埋めない。

## テスト

`src/core/` はテストを先に書く。特に次の三点を重点的に扱う。

- `Rational` の演算と約分、境界（ゼロ除算、負数、大きな分母）
- 循環を含むグラフの求解。石油処理相当の小さなケースを手計算で検証したデータで固定する
- 離散モードの余り在庫の繰り越し

UI のテストは初版では最小限でよい。

## コマンド

```
npm run dev
npm run build
npm run test
npm run typecheck
```

## コーディング規約

- TypeScript は strict。`any` を使わない。型が決まらない箇所は `unknown` から絞る
- 判別共用体には必ず網羅性チェックを入れる（`default` で `never` に代入する）
- 外部から入る値（JSON、URL パラメータ、localStorage）は必ず Zod を通してから型を信じる
- コメントは「なぜそうするか」を書く。「何をしているか」はコードから読める

## 作業の進め方

- 大きな変更に着手する前に、対象ファイルと方針を先に示す
- `SPEC.md` と矛盾する実装が必要になったら、実装を進める前に指摘する
- 仕様が曖昧で判断が必要な箇所は、勝手に決めずに確認する
